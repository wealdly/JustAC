# JustAC 12.0 (Midnight) API Compatibility Analysis

Last Updated: February 2026

## Executive Summary

**JustAC should retain FULL functionality in 12.0** with minimal changes required.

Unlike rotation helpers (Hekili, WeakAuras rotation profiles) that Blizzard is targeting, JustAC:
- Displays Blizzard's own `C_AssistedCombat` suggestions (not calculating rotation)
- Does not make decisions for the player
- Functions as a visual overlay only

### Critical Updates from Blizzard (Alpha 6 / Beta 3):

1. **Player health is NOT secret** - `UnitHealth("player")` and `UnitHealthMax("player")` no longer return secrets (fixed in Alpha 6)
2. **Secondary resources NOT secret** - Combo points, runes, holy power, chi, soul shards, arcane charges
3. **Primary resources ARE secret** - Mana, energy, rage, focus (in combat)
4. **Cooldowns getting flagged** - Blizzard is flagging spells individually as non-secret
5. **New APIs available** - `SetCooldownFromExpirationTime()`, `SetAlphaFromBoolean()`, `C_StringUtil.TruncateWhenZero()`

## API Audit Results

### ✅ SAFE - No Changes Required

| Module | API | Status | Notes |
|--------|-----|--------|-------|
| BlizzardAPI | `C_AssistedCombat.GetNextCastSpell()` | ✅ Safe | Blizzard's own rotation helper - will remain |
| BlizzardAPI | `C_AssistedCombat.GetRotationSpells()` | ✅ Safe | Core to Combat Assistant feature |
| BlizzardAPI | `C_AssistedCombat.IsAvailable()` | ✅ Safe | System availability check |
| ActionBarScanner | `GetBindingKey()` | ✅ Safe | Keybind lookup - not combat sensitive |
| ActionBarScanner | `HasAction()` | ✅ Safe | Action bar state |
| ActionBarScanner | `GetActionBarPage()` | ✅ Safe | UI state |
| UIManager | `CreateFrame()` | ✅ Safe | Frame creation |
| UIManager | `LibCustomGlow` | ✅ Safe | Visual effects only |
| SpellQueue | `C_Spell.GetSpellInfo()` | ✅ Safe | Spell metadata (name, icon) |
| SpellQueue | `C_SpellBook.IsSpellInSpellBook()` | ✅ Safe | Spell availability check |
| JustAC | `UnitHealth("player")` | ⚠️ SECRET in combat | Returns secret in open world combat (verified 2026-02-25). Fallback: LowHealthFrame |
| JustAC | `UnitHealthMax("player")` | ⚠️ SECRET in combat | Same as UnitHealth. Fallback: LowHealthFrame |
| JustAC | `LowHealthFrame:IsShown()` | ✅ Always Safe | Visual frame state - never secret, works as threshold fallback |
| JustAC | `LowHealthFrame:GetAlpha()` | ✅ Always Safe | Visual property - never secret, distinguishes low vs critical |

### ⚠️ CONDITIONAL - May Return Secret Values

| Module | API | 12.0 Behavior | Migration Strategy |
|--------|-----|---------------|-------------------|
| UIRenderer | `C_Spell.GetSpellCooldown()` | All fields SECRET except `isOnGCD` | `isOnGCD` is NeverSecret with three states: `true`=GCD only (ready), `false`=real CD (flagged spells like Judgment/BoJ/Wake), `nil`=ambiguous. Use local CD tracking + action bar fallback for nil case. |
| SpellQueue | `C_SpellActivationOverlay.IsSpellOverlayed()` | May return secret boolean | Use `SetAlphaFromBoolean()` for glow visibility |
| RedundancyFilter | `C_UnitAuras.GetAuraDataByIndex()` | Aura contents secret in combat | **IMPLEMENTED:** auraInstanceID mapping resolves identity (v38) |
| BlizzardAPI | `UnitAffectingCombat()` | Not secret for player | Safe to use |
| FormCache | `GetShapeshiftFormInfo()` | Should remain non-secret | Monitor for changes |
| UIRenderer / UINameplateOverlay | `castBar:IsVisible()` | Returns secret boolean in combat | Use nested-pcall pattern (see below) |
| UIRenderer / UINameplateOverlay | `castBar.BorderShield:IsShown()` | Returns secret boolean in combat | Use nested-pcall pattern (see below) |
| UIRenderer / UINameplateOverlay | `castBar.ImportantCastIndicator:IsShown()` | Returns secret boolean in combat | Use nested-pcall pattern (see below) |

### 🔄 REQUIRES ADAPTATION

#### 1. Cooldown Display (UIManager.lua)

**Current Code:**
```lua
local cooldownInfo = C_Spell.GetSpellCooldown(spellID)
if cooldownInfo and cooldownInfo.duration > 0 then
    icon.cooldown:SetCooldown(cooldownInfo.startTime, cooldownInfo.duration)
end
```

**12.0 Migration:**
```lua
-- Option A: Use new API that accepts secrets
local cooldownInfo = C_Spell.GetSpellCooldown(spellID)
if cooldownInfo then
    icon.cooldown:SetCooldownFromExpirationTime(
        cooldownInfo.startTime + cooldownInfo.duration,
        cooldownInfo.duration
    )
end

-- Option B: Let Blizzard handle it via ActionButton cooldown template
-- (Already works this way for assisted combat buttons)
```

#### 2. Proc Glow Detection (UIManager.lua)

**Current Code:**
```lua
local hasProc = C_SpellActivationOverlay.IsSpellOverlayed and 
                C_SpellActivationOverlay.IsSpellOverlayed(spellID)
```

**12.0 Migration:**
```lua
-- If returns secret, use SetAlphaFromBoolean
local hasProc = C_SpellActivationOverlay.IsSpellOverlayed(spellID)
if issecretvalue and issecretvalue(hasProc) then
    -- Use secret-safe method
    glowFrame:SetAlphaFromBoolean(hasProc, 1.0, 0.0)
else
    -- Normal path (out of combat or API unchanged)
    if hasProc then StartProcGlow() else StopProcGlow() end
end
```

#### 3. Redundancy Filter (RedundancyFilter.lua) — IMPLEMENTED

**Problem:** `C_UnitAuras.GetAuraDataByIndex()` returns secret `spellId`/`name` in combat.

**Solution: auraInstanceID Mapping (v38)**

`auraInstanceID` is **NeverSecret** — a stable numeric handle that maps to the same aura across combat. The RedundancyFilter builds instanceID→spellID maps out of combat, then resolves auras via the maps in combat.

```lua
-- Out of combat: populate instance maps
for i = 1, 40 do
    local data = C_UnitAuras.GetAuraDataByIndex("player", i, "HELPFUL")
    if data then
        instanceToSpellMap[data.auraInstanceID] = data.spellId
        instanceToNameMap[data.auraInstanceID] = data.name
    end
end

-- In combat: resolve via map when spellId is secret
if BlizzardAPI.IsSecretValue(data.spellId) then
    data.spellId = instanceToSpellMap[data.auraInstanceID]
    data.name = instanceToNameMap[data.auraInstanceID]
end
```

**Multi-cycle support:** `UNIT_AURA(unit, updateInfo)` keeps maps current:
- `removedAuraInstanceIDs` → clean map entries, record in `combatRemovedSpellIDs`
- `addedAuras` → map new instances (non-secret spellId) or match via pending activation FIFO queue
- `UNIT_SPELLCAST_SUCCEEDED` → queues pending activations, consumed by `addedAuras` within 2s window
- Harmful auras filtered from pending match via `isHelpful`/`isHarmful` check

#### 4. Cast Bar Secret Booleans (UIRenderer / UINameplateOverlay)

Nameplate cast bar frame methods (`IsVisible()`, `IsShown()`) can return **secret booleans**
in 12.0+.  A secret boolean cannot be used in Lua `and`/`or`/`if` expressions — doing so
taints the calling function and may propagate taint to the UI.

**Problem:** Even wrapping the call in `pcall` is insufficient because `pcall` returns the
secret boolean as its second value.  Using it in `if ok and result then` still triggers taint.

**Solution — Nested pcall pattern:**
```lua
-- Step 1: Call the method safely (may return a secret boolean)
local ok, rawVal = pcall(castBar.IsVisible, castBar)

-- Step 2: Convert secret boolean → regular boolean inside a second pcall.
-- If rawVal is secret, the inner `and` crashes → pcall returns ok=false.
local testOk, safeVal = pcall(function() return rawVal and true or false end)

-- Step 3: Both pcalls must succeed for the value to be trustworthy
local isVisible = ok and testOk and safeVal
```

**Where used:**
- `castBar:IsVisible()` — detect whether target is casting
- `castBar.BorderShield:IsShown()` — detect uninterruptible casts (shield icon)
- `castBar.ImportantCastIndicator:IsShown()` — detect "must interrupt" casts (proc glow)

**Design:** Fail-closed for shield/important (assume interruptible if secret), fail-open for
cast detection (no interrupt icon shown if we can't determine cast state).

## Why JustAC Is Safe

From Blizzard's official statements:

> "We have no issues with addons allowing a player to perform at a competent level, but when an addon allows players to perform at a truly optimal level, it gives those players an unfair advantage. This is why our **Combat Assistant feature is non-optimal by design**."

JustAC displays the Combat Assistant output - the same data Blizzard shows on their own action buttons. We're not:
- Parsing combat log events ❌
- Calculating optimal rotation ❌  
- Making interrupt/cooldown decisions ❌
- Simplifying enemy names ❌

We ARE:
- Showing Blizzard's suggestions with hotkeys ✅
- Visual customization (glows, positioning) ✅
- Displaying cooldowns ✅

## New 12.0 APIs to Leverage

| API | Use Case |
|-----|----------|
| `issecretvalue(val)` | Check if value is secret before comparison |
| `canaccessvalue(val)` | Check if we can read the value |
| `SetAlphaFromBoolean(bool, alphaTrue, alphaFalse)` | Set visibility from secret bool |
| `SetCooldownFromExpirationTime()` | Set cooldown accepting secret values |
| `C_StringUtil.TruncateWhenZero()` | Display numbers that may be secret |

## Migration Timeline

1. **Pre-12.0 (Now)**: Document APIs, add `issecretvalue` checks where beneficial
2. **12.0 Alpha/Beta**: Test on PTR, implement migrations as needed
3. **12.0 Launch**: Ship compatible version

## Code Changes Required

### Priority 1: Add Secret Value Detection Utility

```lua
-- Add to BlizzardAPI.lua
function BlizzardAPI.IsSecretValue(value)
    return issecretvalue and issecretvalue(value) or false
end

function BlizzardAPI.CanAccessValue(value)
    return canaccessvalue and canaccessvalue(value) or true
end
```

### Priority 2: Update Cooldown Display (UIManager.lua)

Add fallback for when cooldown values are secret.

### Priority 3: Graceful Degradation for Aura Checks

Make RedundancyFilter fail-open (show spell if can't check aura).

## Health Detection for Defensives (12.0)

### What Works ✅

**LowHealthFrame Visual Overlay** (Reliable, Non-Secret)
- Blizzard's native low health warning overlay that appears on the player frame
- `LowHealthFrame:IsShown()` - Returns `true` when health drops to ~35% (configurable via CVar)
- `LowHealthFrame:GetAlpha()` - Returns higher alpha (~0.8) at critical health (~20%)
- **Why it works:** Visual frame state, not numeric health data - not affected by secrets
- **Used for:** Defensive spell priority thresholds when exact health unavailable

**UnitHealth API** (SECRET in combat — verified 2026-02-25)
- `UnitHealth("player")` and `UnitHealthMax("player")` are SECRET in open world combat
- **Previous assumption was wrong:** Earlier testing suggested "usually safe" — this is NOT the case
- **LowHealthFrame is the primary health signal in combat**

**Current Implementation:**
```lua
function BlizzardAPI.GetPlayerHealthPercentSafe()
    local health = UnitHealth("player")
    local maxHealth = UnitHealthMax("player")
    
    -- Try exact health first (usually works)
    if health and maxHealth and maxHealth > 0 then
        if not (issecretvalue and (issecretvalue(health) or issecretvalue(maxHealth))) then
            return (health / maxHealth) * 100
        end
    end
    
    -- Fallback to visual overlay when secrets block API
    local lowHealthState = GetLowHealthState()
    if lowHealthState == "CRITICAL" then
        return 20  -- Approximate
    elseif lowHealthState == "LOW" then
        return 35  -- Approximate
    else
        return 100  -- Assume healthy
    end
end
```

### What Doesn't Work ❌

**StatusBar GetValue/GetMinMaxValues** (Frequently Secret)
- `PlayerFrame.healthBar:GetValue()` and `:GetMinMaxValues()` often return secrets
- Cannot reliably compute percentage from these values
- **Reason:** These are the same underlying values as UnitHealth, subject to same secrecy

**StatusBar Texture Coordinates** (Secret-Dependent)
- `PlayerFrame.healthBar.texture:GetTexCoord()` returns secrets when health is secret
- Cannot perform arithmetic on secret coordinates to calculate fill percentage
- **Reason:** Texture coordinates derived from secret health values

**Texture Width Measurements** (Unreliable)
- Attempted to measure visual fill width: `texture:GetWidth()` or `texture:GetRight() - texture:GetLeft()`
- **Problem:** Texture size doesn't reliably reflect health percentage
- Layout/scaling issues made measurements inconsistent

**Widget Value Anchoring** (Complex, Abandoned)
- Attempted to mirror player health bar by anchoring a custom bar to the source texture
- **Problem:** Requires complex texture coordinate math that becomes secret-dependent
- Layout edge cases and scaling made this approach fragile

**Health Bar UI Clone Attempt** (v3.12 Development, Abandoned)
- Created `UIHealthBar.lua` module attempting to display numeric health percentage
- Tried multiple detection methods: GetValue, texcoords, width, anchoring
- **Conclusion:** Could not guarantee non-secret numeric percentage in all scenarios
- Module was marked abandoned and later removed from codebase

### Best Practice (Current Implementation)

Use a **tiered approach** for defensive spell detection:

1. **Try exact health** via `UnitHealth()` - works in most situations
2. **Fall back to visual overlay** (`LowHealthFrame`) when secrets block exact health
3. **Design defensive logic** to work with approximate thresholds rather than requiring exact percentages

**Priority System (Does Not Require Exact Health):**
- **Procs** → Always show at any health (Victory Rush, Regrowth proc, etc.)
- **Low (~35%)** → Show big self-heals
- **Critical (~20%)** → Show major cooldowns > potions > heals

This approach works because:
- Proc detection is event-driven (not dependent on health)
- Threshold-based priorities tolerate approximate health ranges
- Visual overlay state is always accessible (never secret)

## Combat-Safe Signal Reference

Authoritative matrix of what data is readable in combat without triggering secrets.
Verified via in-game `/script` testing. **Update this section whenever new signals are tested.**

Last verified: 2026-02-25 (WoW 12.0.1)

### Unit APIs (target)

| API | Returns | Secret? | Verified | Notes |
|-----|---------|---------|----------|-------|
| `UnitClassification("target")` | `"normal"`, `"elite"`, `"worldboss"`, `"rare"`, `"rareelite"`, `"minus"` | **No** | 2026-02-24 | NeverSecret. Use for boss/elite/trivial detection. |
| `UnitIsUnit("target", "boss1-5")` | boolean | **No** | 2026-02-23 | NeverSecret. Dungeon/raid boss detection. |
| `UnitIsPlayer("target")` | boolean | **No** | 2026-02-24 | NeverSecret (confirmed `issecretvalue()=false`). Player vs NPC. |
| `UnitIsMinion("target")` | boolean | **No** | 2026-02-24 | NeverSecret. Pets, totems, treants, guardians. |
| `UnitCreatureType("target")` | string | **YES** | 2026-02-23 | SECRETED in combat. Cache OOC only. |
| `UnitGUID("target")` | string | **YES** | 2026-02-23 | SECRETED in combat. GUID-keyed caches not viable. |
| `UnitCreatureFamily("target")` | string/nil | **No** | 2026-02-23 | NeverSecret, but only distinguishes Beast from everything else. |
| `UnitThreatSituation("player", "target")` | 0-3 | **No** | 2026-02-24 | NeverSecret. 0=none, 1=not tanking, 2=tanking insecure, 3=tanking. |
| `UnitIsCrowdControlled("target")` | boolean | **No** | 2026-02-24 | NeverSecret (when API exists). |
| `UnitCanAttack("player", "target")` | boolean | **No** | pre-12.0 | Standard hostility check. |
| `UnitExists("target")` | boolean | **No** | pre-12.0 | Standard existence check. |
| `UnitIsDead("target")` | boolean | **No** | pre-12.0 | NeverSecret (confirmed for pet rez logic). |

### Unit APIs (player)

| API | Returns | Secret? | Verified | Notes |
|-----|---------|---------|----------|-------|
| `UnitHealth("player")` | number | **YES** | 2026-02-25 | SECRET in open world combat (verified). Fallback: LowHealthFrame. |
| `UnitHealthMax("player")` | number | **YES** | 2026-02-25 | SECRET in combat (verified). Same fallback. |
| `UnitPower("player")` (default) | number | **YES** | 2026-02-25 | SECRET — returns primary resource (mana/energy/rage/focus) which is always secreted. |
| `UnitPower("player", type)` (continuous) | number | **YES** | 2026-02-25 | Continuous resources (0=Mana, 1=Rage, 2=Focus, 3=Energy, 6=RunicPower) are SECRET. |
| `UnitPower("player", type)` (discrete) | number | **No** | 2026-02-25 | Discrete secondary resources (4=ComboPoints, 9=HolyPower, 7=SoulShards, 12=Chi, 16=ArcaneCharges) are **NeverSecret**. Verified: CP on Rogue, HP on Ret Paladin. |
| `UnitPowerMax("player"[, type])` | number | **No** | 2026-02-25 | NeverSecret for ALL power types. Cacheable at combat exit. |
| `UnitPowerType("player")` | number | **No** | 2026-02-25 | NeverSecret. Returns primary power type enum. |
| `GetComboPoints("player","target")` | number | **No** | 2026-02-25 | **NeverSecret** (verified on Rogue, corrects earlier erroneous SECRET finding). Equivalent to `UnitPower("player", 4)`. |
| `UnitAffectingCombat("player")` | boolean | **No** | pre-12.0 | Standard combat detection. |
| `UnitClass("player")` | string, string | **No** | pre-12.0 | Static metadata. |
| `GetShapeshiftFormInfo()` | form data | **No** | pre-12.0 | Monitor for changes. |

### Unit APIs (target health/power)

| API | Returns | Secret? | Verified | Notes |
|-----|---------|---------|----------|-------|
| `UnitHealth("target")` | number | **YES** | 2026-02-25 | SECRET in combat. |
| `UnitHealthMax("target")` | number | **YES** | 2026-02-25 | SECRET in combat. |
| `UnitPower("target")` | number | **YES** | 2026-02-25 | SECRET in combat. |
| `UnitPowerMax("target")` | number | **YES** | 2026-02-25 | SECRET in combat. |

### Nameplate Frame Fields (target)

Blizzard's nameplate code caches computed state as plain Lua table fields on setup.
Reading these fields is **not an API call** — they're regular table values set from
NeverSecret APIs during `OnUnitSet`, and remain readable in combat.

| Field Path | Type | Secret? | Verified | Notes |
|------------|------|---------|----------|-------|
| `nameplate.UnitFrame.isPlayer` | boolean | **No** | 2026-02-24 | Set from `UnitIsPlayer()`. |
| `nameplate.UnitFrame.isFriend` | boolean | **No** | 2026-02-24 | Set from `UnitIsFriend()`. |
| `nameplate.UnitFrame.isSimplified` | boolean | **No** | 2026-02-24 | User display pref, not mob property. |
| `nameplate.UnitFrame.isDead` | boolean | **No** | — | Set from `UnitIsDead()`. |
| `nameplate.UnitFrame.isTarget` | boolean | **No** | — | Derived from selectionHighlight. |
| `nameplate.UnitFrame.ClassificationFrame.classificationAtlasElement` | string/nil | **No** | 2026-02-24 | Nil for normal mobs. Redundant with `UnitClassification()`. |

### Nameplate Cast Bar (target) — requires mob to be casting

| Field / Method | Type | Secret? | Verified | Notes |
|----------------|------|---------|----------|-------|
| `castBar:IsVisible()` | boolean | **YES** | 2026-02 | Returns secret boolean. Use nested-pcall pattern. |
| `castBar.Icon:IsShown()` | boolean | **No** | 2026-02 | NeverSecret on nameplate castbars (HideIconWhenNotInterruptible). Primary interruptibility signal. |
| `castBar.BorderShield:IsShown()` | boolean | **YES** | 2026-02 | May return secret boolean. Use nested-pcall. Fallback interruptibility signal. |
| `castBar.ImportantCastIndicator:IsShown()` | boolean | **UNTESTED** | — | Blizzard's "important cast" flag. Needs in-combat test during a flagged cast. |
| `castBar.CastTargetNameText:GetText()` | string/nil | **UNTESTED** | — | Cast target name. Needs in-combat test during a targeted cast. |

### Spell APIs

| API | Returns | Secret? | Verified | Notes |
|-----|---------|---------|----------|-------|
| `C_Spell.GetSpellInfo(id)` | SpellInfo table | **No** | pre-12.0 | Spell metadata (name, icon). |
| `C_Spell.GetSpellCooldown(id).isOnGCD` | true/false/nil | **No** | 2026-02-25 | NeverSecret. Three states: `true`=GCD only (ready), `false`=real CD (only for Blizzard-flagged spells, e.g. Judgment/BoJ/Wake), `nil`=absent (off CD OR unflagged spell on CD). Major CDs (Divine Toll, Shadow Blades) never show `false`. |
| `C_Spell.GetSpellCooldown(id).duration` | number | **YES** | 2026-02-25 | Blanket-secreted in combat (even when 0). |
| `C_Spell.GetSpellCooldown(id).startTime` | number | **YES** | 2026-02-25 | Blanket-secreted in combat. |
| `C_Spell.GetSpellCooldown(id).isEnabled` | boolean | **YES** | 2026-02-25 | Secreted in combat. |
| `C_Spell.GetSpellCooldown(id).modRate` | number | **YES** | 2026-02-25 | Secreted in combat. |
| `C_Spell.GetSpellCooldown(id).activeCategory` | number | **YES** | 2026-02-25 | CD category ID (e.g. 2285 for Wake). Secreted in combat. |
| `C_Spell.GetSpellCooldown(id).timeUntilEndOfStartRecovery` | number? | **YES** | 2026-02-25 | Countdown timer — tracks GCD remaining (for GCD-only spells) or full CD remaining (for real-CD spells). Secreted in combat. Display-only via UI pipeline. |
| `C_Spell.IsSpellInRange(id, target)` | boolean | **No** | 2026-02-25 | NeverSecret. Real bool in combat. Confirmed `type()=boolean`, `issecretvalue()=false` on Sub Rogue in combat. No need for `IsActionInRange` fallback. |
| `C_Spell.IsSpellUsable(id)` | boolean, boolean | **No** | 2026-02-25 | **NeverSecret** (verified Ret Paladin). Returns usable + noMana. |
| `C_Spell.GetSpellPowerCost(id)` | PowerCostInfo[] | **No** | 2026-02-25 | **ALL fields NeverSecret**: `type`, `cost`, `minCost`, `costPercentOfMax`. Cacheable at registration. |
| `C_Spell.GetSpellCharges(id)` | SpellChargeInfo | **YES** | 2026-02-25 | **ALL fields SECRET** including `maxCharges`, `currentCharges`, `cooldownStartTime`, `cooldownDuration`, `chargeModRate`. Cache `maxCharges` out of combat. |
| `C_Spell.IsCurrentSpell(id)` | boolean | **No** | 2026-02-25 | NeverSecret. Active cast/channel detection. |
| `C_SpellBook.IsSpellInSpellBook(id)` | boolean | **No** | pre-12.0 | Spell availability. |
| `FindSpellOverrideByID(id)` | number | **No** | pre-12.0 | Talent/spec override resolution. |

### Aura APIs

| API / Field | Returns | Secret? | Verified | Notes |
|-------------|---------|---------|----------|-------|
| `C_UnitAuras.GetAuraDataByIndex().auraInstanceID` | number | **No** | 2026-02 | NeverSecret. Stable handle for aura identity in combat. |
| `C_UnitAuras.GetAuraDataByIndex().spellId` | number | **YES** | 2026-02 | Secreted in combat. Resolve via instanceID→spellID map. |
| `C_UnitAuras.GetAuraDataByIndex().name` | string | **YES** | 2026-02 | Secreted in combat. |
| `C_UnitAuras.GetAuraDataByIndex().duration` | number | **YES** | 2026-02-25 | Secreted in combat. |
| `C_UnitAuras.GetAuraDataByIndex().expirationTime` | number | **YES** | 2026-02-25 | Secreted in combat. |
| `C_UnitAuras.GetAuraDataByIndex().applications` | number | **YES** | 2026-02-25 | Stack count — secreted in combat. |
| `C_UnitAuras.GetAuraDataByIndex().sourceUnit` | string | **YES** | 2026-02-25 | Secreted in combat. |
| `C_UnitAuras.GetAuraDataByIndex().isStealable` | boolean | **YES** | 2026-02-25 | Secreted in combat. |
| `C_UnitAuras.GetAuraDataByIndex().isFromPlayerOrPlayerPet` | boolean | **YES** | 2026-02-25 | Secreted in combat. |
| `C_UnitAuras.GetAuraDataByIndex().dispelName` | string/nil | **No** | 2026-02-25 | **NeverSecret**. Dispel type ("Magic", "Curse", etc.). Useful for purge/cleanse. |
| `C_UnitAuras.GetAuraDataByIndex().isHelpful` | boolean | **Mostly No** | 2026-02 | Usually readable. Fail-open. |

### Action Bar APIs

| API | Returns | Secret? | Verified | Notes |
|-----|---------|---------|----------|-------|
| `C_ActionBar.IsUsableAction(slot)` | boolean, boolean | **No** | 2026-02 | Visual icon state — usable + noMana. |
| `C_ActionBar.IsActionInRange(slot, target)` | boolean? | **No** | 2026-02-25 | Real bool in combat. `nil` if no target/no range check. |
| `C_ActionBar.IsInterruptAction(slot)` | boolean | **No** | 2026-02-25 | Identifies interrupt spell slots. Works in combat. |
| `C_ActionBar.IsCurrentAction(slot)` | boolean | **No** | 2026-02-25 | `true` during active cast/channel/toggle only. NOT melee swing. Momentary. |
| `C_ActionBar.IsAttackAction(slot)` | boolean | **No** | 2026-02-25 | Auto-attack slot detection. Verified: slot 27 on Rogue. |
| `C_ActionBar.IsAutoRepeatAction(slot)` | boolean | **No** | 2026-02-25 | Auto-repeat detection (Shoot/Wand). Returns `-` for melee classes. |
| `C_ActionBar.HasAction(slot)` | boolean | **No** | 2026-02-25 | Slot population check. |
| `GetActionCount(slot)` | number | **YES** | 2026-02-25 | Reagent/stack counts — secreted in combat. |
| `GetActionTexture(slot)` | texturePath | **No** | 2026-02-25 | Icon texture — NeverSecret. |
| `C_ActionBar.EnableActionRangeCheck(slot, bool)` | void | **No** (AllowedWhenUntainted) | 2026-02-25 | Opt-in for `ACTION_RANGE_CHECK_UPDATE`. Works from addon code. |
| `ACTION_USABLE_CHANGED` event | ActionUsableState[] | **No** | 2026-02-25 | Batched per-slot usable/noMana bools. Fires during GCD/CD transitions. |
| `ACTION_RANGE_CHECK_UPDATE` event | slot, isInRange, checksRange | **No** | 2026-02-25 | Push-based range notifications. `checksRange=false` on target loss. Very chatty. |
| `ACTIONBAR_UPDATE_COOLDOWN` event | (none) | N/A | 2026-02-25 | **Fires every frame (~15-18Hz)**. No payload. Useless as a discrete signal — use `SPELL_UPDATE_COOLDOWN` instead. |
| `SPELL_UPDATE_COOLDOWN` event | spellID, baseSpellID, category, startRecoveryCategory | **No** | 2026-02-25 | **spellID is NeverSecret in combat.** Fires per-spell on CD state change. `startRecoveryCategory=133` = GCD. `nil` spellID = batch "refresh all". Fires ~10× per cast (GCD cascade). Duplicate per-spell events (base + override). |
| `SPELL_UPDATE_USABLE` event | (none) | N/A | 2026-02-25 | Fires when spell usability changes (CD expire, resource gain). No payload. |
| `SPELL_RANGE_CHECK_UPDATE` event | spellIdentifier, isInRange, checksRange | **No** (from source) | untested | Spell-level push-based range. Requires `C_Spell.EnableSpellRangeCheck(spellID, true)` opt-in. Parallel to `ACTION_RANGE_CHECK_UPDATE`. |
| `GetBindingKey()` | string | **No** | pre-12.0 | Keybind lookup — not combat sensitive. |

### Spell Classification APIs (12.0+)

| API | Returns | Secret? | Verified | Notes |
|-----|---------|---------|----------|-------|
| `C_Spell.IsExternalDefensive(spellID)` | boolean | **No** | 2026-02-25 | Static classification. Guardian Spirit, Pain Supp, Ironbark, etc. |
| `C_CooldownViewer.GetCooldownViewerCategorySet(cat, allowUnlearned)` | cooldownID[] | **No** | 2026-02-25 | Blizzard's spell taxonomy. 0=Essential, 1=Utility, 2=TrackedBuff, 3=TrackedBar. |
| `C_CooldownViewer.GetCooldownViewerCooldownInfo(cooldownID)` | CooldownViewerCooldown | **No** | 2026-02-25 | Static metadata: spellID, overrideSpellID, isKnown, category, flags. `hasAura` is config flag, NOT live state. |
| `C_CooldownViewer.IsCooldownViewerAvailable()` | boolean, reason | **No** | — | Feature availability check. |
| `C_UnitAuras.GetCooldownAuraBySpellID(spellID)` | number? | **No** | 2026-02-25 | AllowedWhenTainted. Returns 0 for standard spells. Only useful for aura-linked CDs. |

### Loss of Control APIs (12.0+)

| API / Field | Returns | Secret? | Verified | Notes |
|-------------|---------|---------|----------|-------|
| `C_LossOfControl.GetActiveLossOfControlDataCount()` | number | **Expected No** | — | Count of active CC effects. |
| `C_LossOfControl.GetActiveLossOfControlData(index).locType` | cstring | **No** (source) | — | NeverSecret per source. CC type: STUN, SILENCE, ROOT, FEAR, etc. |
| `C_LossOfControl.GetActiveLossOfControlData(index).priority` | number | **No** (source) | — | NeverSecret per source. CC priority ranking. |
| `C_LossOfControl.GetActiveLossOfControlData(index).displayType` | number | **No** (source) | — | NeverSecret per source. Visual display type. |
| `C_LossOfControl.GetActiveLossOfControlData(index).auraInstanceID` | number? | **No** (source) | — | NeverSecret per source. Links to aura instance map. |
| `C_LossOfControl.GetActiveLossOfControlData(index).spellID` | number | **YES** (source) | — | Expected secret in combat. |
| `C_LossOfControl.GetActiveLossOfControlData(index).duration` | number | **YES** (source) | — | Expected secret in combat. |
| `LOSS_OF_CONTROL_ADDED` event | unitTarget, effectIndex | **Expected No** | — | Fires when CC applied. |
| `PLAYER_CONTROL_LOST` / `PLAYER_CONTROL_GAINED` events | — | **No** | pre-12.0 | Binary CC state. |

### LuaDurationObject APIs (12.0+)

| API / Method | Returns | Secret? | Verified | Notes |
|-------------|---------|---------|----------|-------|
| `C_Spell.GetSpellCooldownDuration(id)` | LuaDurationObject | **Exists** | 2026-02-25 | Returns object; use methods below. |
| `C_ActionBar.GetActionCooldownDuration(slot)` | LuaDurationObject | **Exists** (no SecretWhen) | 2026-02-25 | Action bar path — same opaque object. |
| `C_ActionBar.GetActionChargeDuration(slot)` | LuaDurationObject | **Exists** (no SecretWhen) | — | Charge recharge duration. |
| `duration:IsZero()` | boolean | **YES** | 2026-02-25 | SECRET in combat (both spell and action bar paths). Cannot use for readiness detection. |
| `duration:HasSecretValues()` | boolean | **No** | 2026-02-25 | NeverSecret, but returns `true` for ALL spells in combat (blanket, not per-spell). Not useful for CD detection. |
| `duration:GetRemainingDuration()` | number | **YES** | — | Expected secret in combat. |
| `Cooldown:SetCooldownFromDurationObject(dur)` | void | — | 2026-02-25 | Accepts LuaDurationObject for opaque CD display. Works in and out of combat. |

**LuaDurationObject Display Pipeline (verified 2026-02-25):**
The intended 12.0 cooldown display pattern. Bypasses secret values entirely — the object is opaque:
```lua
-- Action bar path (no SecretWhen on GetActionCooldownDuration):
local dur = C_ActionBar.GetActionCooldownDuration(slot)
cooldown:SetCooldownFromDurationObject(dur)

-- Spell path (also works):
local dur = C_Spell.GetSpellCooldownDuration(spellID)
cooldown:SetCooldownFromDurationObject(dur)
```
The Duration object's internal values are secret (`IsZero()` returns SECRET), but the Cooldown widget renders the sweep animation correctly. This is a **display-only pipe** — you cannot branch on the values, but you CAN show accurate cooldown sweeps.

### C_Secrets Pre-Flight APIs

| API | Returns | Secret? | Verified | Notes |
|-----|---------|---------|----------|-------|
| `C_Secrets.HasSecretRestrictions()` | boolean | **No** | 2026-02-25 | `true` when secrecy system active. |
| `C_Secrets.ShouldCooldownsBeSecret()` | boolean | **No** | 2026-02-25 | `true` in combat. Blanket, not per-spell. |
| `C_Secrets.ShouldSpellCooldownBeSecret(id)` | boolean | **No** | 2026-02-25 | `true` for ALL spells in combat — even flagged ones that return `isOnGCD`. |
| `C_Secrets.GetSpellCooldownSecrecy(id)` | number | **No** | 2026-02-25 | Returns `2` for all spells in combat. No per-spell distinction. |

### Instance / Context APIs

| API | Returns | Secret? | Verified | Notes |
|-----|---------|---------|----------|-------|
| `IsInInstance()` | boolean, instanceType | **No** | pre-12.0 | `"pvp"`, `"arena"`, `"party"`, `"raid"`, `"none"`. |
| `C_PvP.IsPVPMap()` | boolean | **No** | pre-12.0 | PvP context detection. |
| `GetTime()` | number | **No** | pre-12.0 | Always available. |
| `C_DamageMeter.GetSessionDurationSeconds(type)` | number? | **No** | 2026-02-25 | Real combat timer. Returns elapsed seconds (e.g., 434). No SecretWhen. |
| `LowHealthFrame:IsShown()` | boolean | **No** | 2026-02 | Visual frame — NeverSecret. ~35% health threshold. |
| `LowHealthFrame:GetAlpha()` | number | **No** | 2026-02 | Higher alpha (~0.8) at ~20% critical. |
| `PlayerCastingBarFrame.channeling` | boolean | **No** | 2026-03 | Plain Lua field on CastingBarMixin — `true` during channels, `nil` otherwise. Replaces removed `PlayerChannelBarFrame`. |

### How to Test New Signals

Run in-game while in combat:
```lua
/script local v = API_CALL_HERE; print("value:", v, "secret:", issecretvalue(v))
```

For nameplate fields:
```lua
/script local np=C_NamePlate.GetNamePlateForUnit("target",false); local uf=np and np.UnitFrame; print("field:", uf and uf.FIELD_NAME)
```

Update this matrix with the result, date, and any caveats.

## Risk Assessment

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| C_AssistedCombat restricted | Very Low | Critical | Would require complete redesign - but this is Blizzard's own feature |
| Cooldown display breaks | Low | Low | Use new `SetCooldownFromExpirationTime()` API - accepts secrets |
| Proc detection breaks | Medium | Low | Use `SetAlphaFromBoolean()` for glow visibility |
| Aura checks break | High | Very Low | **Mitigated:** auraInstanceID mapping + pending activation queue |
| Hotkey detection breaks | Very Low | High | GetBindingKey is not combat-sensitive |
| Player health secret | **Medium** | Medium | LowHealthFrame overlay provides fallback thresholds |
| Defensive suggestions break | Low | Medium | Falls back to approximate thresholds when secrets block exact health |

## JustAC Feature Availability Matrix

| Feature | 12.0 Status | Notes |
|---------|-------------|-------|
| Core spell suggestions | ✅ Works | C_AssistedCombat is Blizzard's feature |
| Hotkey display | ✅ Works | Binding APIs not combat-sensitive |
| Defensive suggestions | ✅ Works | UnitHealth usually safe; LowHealthFrame fallback for thresholds |
| Health percentage display | ❌ Not Reliable | Cannot guarantee non-secret numeric percentage in all scenarios |
| Redundancy filter | ✅ Works | auraInstanceID mapping resolves auras in combat (v38) |
| Proc glow detection | ⚠️ Degraded | Use SetAlphaFromBoolean if secret |
| Cooldown display | ⚠️ Partial | Use SetCooldownFromExpirationTime |
| Form detection | ✅ Works | GetShapeshiftFormInfo not restricted |
| Health text on icons | ✅ Works | UnitHealth("player") confirmed safe |

## Implementation Status

### ✅ Already Implemented

- `BlizzardAPI.IsSecretValue()` - Check if value is secret
- `BlizzardAPI.IsDefensivesFeatureAvailable()` - Test health API access
- `BlizzardAPI.IsRedundancyFilterAvailable()` - Test aura API access
- `BlizzardAPI.RefreshFeatureAvailability()` - Force re-check on login
- RedundancyFilter fail-open behavior
- `/jac modules` shows feature availability status
- `BlizzardAPI.IsSpellUsable()` - Falls back to `C_ActionBar.IsUsableAction()` when secrets detected
- `ActionBarScanner.GetSlotForSpell()` - Gets action bar slot for spell (for action bar fallback)
- `BlizzardAPI.GetPlayerHealthPercentSafe()` - Returns exact health when available, falls back to LowHealthFrame thresholds
- `BlizzardAPI.GetLowHealthState()` - Returns "NONE", "LOW" (~35%), or "CRITICAL" (~20%) based on visual overlay
- **Cooldown readiness via `isOnGCD`** - NeverSecret three-state field replaces secreted `duration`/`startTime` comparisons. Used in `BlizzardAPI.IsSpellReady()`, `IsSpellOnGCD()`, `IsSpellOnRealCooldown()`, `SpellDB.IsInterruptOnCooldown()`
- **auraInstanceID mapping** (RedundancyFilter v38) - Builds instanceID→spellID/name/icon/timing maps out of combat; resolves auras in combat via maps; `OnUnitAuraUpdate(updateInfo)` keeps maps current with addedAuras/removedAuraInstanceIDs
- **Combat removal tracking** - `combatRemovedSpellIDs` prevents trusted cache merge from re-adding removed buffs; spellStateKnown bypass on non-DPS filter gate
- **Pending activation queue** - FIFO bridge from `UNIT_SPELLCAST_SUCCEEDED` → `UNIT_AURA addedAuras` for mapping new instances when spellId is secret; 2s window, harmful aura filtering
- **Cast bar secret boolean handling** - Nested pcall pattern for `castBar:IsVisible()`, `BorderShield:IsShown()` (importance uses `C_Spell.IsSpellImportant(spellID)` API instead of `ImportantCastIndicator:IsShown()`)

### Action Bar Usability Fallback (12.0)

When `C_Spell.IsSpellUsable()` returns secret values for `isUsable` or `notEnoughResources`, we now:

1. Look up the action bar slot for the spell via `ActionBarScanner.GetSlotForSpell()`
2. Call `C_ActionBar.IsUsableAction(slot)` to check the visual icon state
3. If action bar state is non-secret (it's visible state, not combat data), use that result
4. If no slot found or action bar also secret, fail-open (assume usable)

This allows the blue tint for "not enough resources" to work even when the spell usability API is secret, because the action bar icon desaturation is a **visual property** that Blizzard allows addons to read.

### 🔲 TODO for 12.0 Launch

1. Update UIManager to use `SetCooldownFromExpirationTime()` for cooldown display
2. Update proc detection to use `SetAlphaFromBoolean()` when secret
3. Add testing CVars support: `secretAurasForced`, `secretCooldownsForced`
4. Test in Midnight beta with test encounter in MOTHERLODE!!

## Conclusion

JustAC is well-positioned for 12.0 because:

1. **We consume Blizzard's Combat Assistant** - they won't break their own feature
2. **We don't do rotation calculation** - just display what Blizzard tells us
3. **Visual APIs remain accessible** - addons can still customize look/feel
4. **Hotkey lookups are safe** - binding information isn't combat-sensitive
5. **Player health is confirmed safe** - UnitHealth("player") NOT secret (Alpha 6+)
6. **Action bar icon state is visible** - desaturation/color reflects usability

The worst-case scenario is reduced functionality in the redundancy filter and proc detection - but the core functionality (showing suggestions with hotkeys) will work.

## References

- [Patch 12.0.0/Planned API changes](https://warcraft.wiki.gg/wiki/Patch_12.0.0/Planned_API_changes) - Blizzard's weekly updates
- [Patch 12.0.0/API changes](https://warcraft.wiki.gg/wiki/Patch_12.0.0/API_changes) - Technical API documentation
- [Midnight Public Beta Update (Nov 13)](https://discord.com/channels/327414731654692866/1438595756083904614) - Philosophy explanation
- [Beta 6 Changes (Jan 12)](https://discord.com/channels/327414731654692866/1460387750480969840) - Latest API updates
